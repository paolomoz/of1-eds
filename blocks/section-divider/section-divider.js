/**
 * section-divider — visual-only separator. All styling lives in page CSS.
 * This block exists solely so EDS's decorateBlocks doesn't 404 on the marker.
 */
export default function decorate() {}
