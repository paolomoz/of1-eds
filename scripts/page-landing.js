/**
 * Landing page has no bespoke interactive devices — this stub exists so the
 * dynamic `import('/scripts/page-landing.js')` in scripts.js doesn't 404.
 *
 * If we ever add landing-specific interactive behavior (e.g. scroll animations
 * on the manifesto statements), it goes here.
 */
